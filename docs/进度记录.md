# cjh 进度记录（防上下文丢失）

> 最后更新：2026-08-21 23:10
> 用途：记录已完成事项与当前状态，中断后可据此恢复。

## 当前状态：P0 MVP + P1 配置/会话完成 + cjterm 抽离

### 已完成
1. **P0 MVP**：agent 核心（消息状态机+工具协议+迭代）、3 工具（bash/read_file/write_file）、
   OpenAI 兼容协议（SSE 流式+工具分片累积）、mock 离线验证、TUI（差分渲染+termios）、
   CLI 模式、--mock 模式 —— 全部端到端验证通过
2. **P1 配置系统**：`~/.cjh/` 目录（settings.json + auth.json + sessions/ + CJH_CONFIG_DIR 覆盖）、
   env 变量覆盖、无 key 提示 auth.json 路径
3. **P1 会话持久化**：JSON 消息历史（全角色）、--list 列表、--resume 恢复、紧凑会话 ID
4. **三库拆分**：cjterm（终端 UI）+ cjllm（LLM 客户端）+ cjcfg（配置/会话），主项目 path 依赖
5. **模块规划**：docs/模块规划.md（拆分原则/候选/操作清单）

### 关键文件
- `src/main.cj` — 入口（--mock/--cli/--resume/--list/默认 TUI）
- `src/config/config.cj` + `session.cj` + `env_ffi.cj` — 配置与会话
- `libs/cjterm/` — 独立终端库（cjpm.toml + src/{ansi,screen,term}.cj）
- `src/{agent,llm,tools,model,tui}/` — 核心包
- `docs/开发文档与踩坑记录.md` — 架构 + 全部踩坑（30+ 条）

### 当前 todo
- [ ] 更新 README（cjterm 库、配置/会话用法）
- [ ] git 提交推送（cjh 私有仓）
- [ ] P1 剩余：SSE 实时上屏、Anthropic 协议、grep 工具

### 构建命令
```bash
source /root/test/cj/tauri_cj/cj-env.sh
cd /root/test/cj/cjh
cjpm build   # 无需编译 C 桥（cjterm 纯 FFI）
# 运行需 LD_LIBRARY_PATH 含 stdx + cangjie runtime
```

### 已知小问题
- CLI 输入 "exit" 会作为用户消息进历史（可优化：exit 不入历史）
- --resume 走 TUI（需要真实终端），CLI 无 resume
