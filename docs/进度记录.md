# cjh 进度记录（防上下文丢失）

> 最后更新：2026-08-26
> 用途：记录已完成事项与当前状态，中断后可据此恢复。
>
> **总览**：版本 v1.3.0 · main 同步至 `ee8919a`（待推送 V2d 并发引擎完整形态）
> - ✅ V1（agent 核心 + 工具 + 双协议 + TUI + 会话 + mock）
> - ✅ V2a（三域 capability + 审批链）· V2c（Compaction + AGENTS.md）· 树形会话 · `--mode json` · Ollama · 6 套主题
> - ✅ V2b Step 1+2（plugin.json + Shell 工具插件 + on_tool_result 事件钩子）
> - ✅ V2b MCP 扩展点（McpClient stdio 传输 + McpTool 代理转发 + McpManager 多服务器管理）
> - ✅ Web 支持实现方案 Step 1-5（HTTP server + WebSocket 流式对话 + 前端 app.js + REST API + Markdown 渲染 + auth_token 鉴权 + 启动安全审计日志）
> - ✅ Web 端到端联调（`/ws` 404 根因修复 = loadMcpServers 阻塞启动；WS 协议端到端验证通过）
> - ✅ 工具结果截断与回溯（落盘 + 回溯钥匙，对标 Pi spill extension）
> - ✅ V2d 并发引擎完整形态（DAG 依赖分析 + 拓扑分组调度 + Provider 连接预热 + 性能基线测量）
> - ⚠️ V2e 部分（`--mode json` 无头模式，缺 Channel/IM 渠道）
> - ⚠️ V3b 部分（Ollama 本地模型，缺 Provider Registry/模型能力描述）
> - ❌ V3 信任链 · V4 多 agent · V2b Step 3（WASM 沙箱 + 中心仓）· Web TLS（`ServerBuilder.tlsConfig` API 待确认）
> - **下一步 ★★★**：V3 信任链（插件签名 + 内容校验和 + 发布者信任列表）→ V2e IM 网关 → V2b Step 3（WASM + 中心仓）
> - **技术债**：偶发 UTF-8 异常（已加日志堆栈，等复现定位）

## 当前状态：V2d 并发引擎完整形态已完成，待提交推送

### 本次会话（2026-08-26，第八轮）完成事项
> 主题：Web `/ws` 404 修复 + Web 端到端联调 + V2d 并发引擎完整形态

**1. Web `/ws` 404 根因修复**（`src/main.cj`）

- **根因**：`loadMcpServers` → `connect()` → `recv()` 永久阻塞（`echo-mcp-server.sh` 启动无响应，`recv()` 逐字节读 stdout 无超时），整个 `main web` 启动卡死。之前所有 `/ws` 测试都命中 Aug25 残留旧进程（PID 1789744，占着 8765 端口），新二进制根本起不来
- **修复**：`loadMcpServers` 用 `spawn` 隔离 `connectAll`，`Future.get(Duration.second * 5)` 超时 catch。单个 MCP 服务器卡住不再阻塞 web server 启动，5s 超时后跳过 MCP 继续
- **验证**：干净进程（杀掉所有残留 main）+ 全新端口 9876，`/ws` 从 404 变成 400（`WebSocket.upgradeFromServer` 对非 WS 请求的正确响应）

**2. Web 端到端联调验证通过**

- **WebSocket 协议端到端**（python WS 客户端连 `ws://127.0.0.1:9876/ws`）：发 `ChatRequest` → 收 `tool_start` → `tool_result` → 流式 `delta`（28 帧）→ `done`（含 token 统计 `rounds:2, tools:1, duration_ms:3283, tokens:3899, cached:1920`）
- **前端资源完整性**：`/` 返回完整 `index.html`（7 个关键标记），`/assets/app.js` 含全部 7 个关键函数（16 次匹配），`/assets/styles.css` 含全部关键选择器（14 次匹配）
- **结论**：v1.3.0 Web 支持彻底闭环

**3. V2d 并发引擎完整形态**（`src/agent/loop.cj` + `libs/cjllm/src/openai.cj`）

- **DAG 依赖分析**（`extractResourceAccess` + `buildDependencyGraph`）：
  - 从 `ToolCall` 提取资源访问 `(path, isWrite)`
  - 资源识别规则：`read_file`/`grep`/`list_dir` → path, read；`write_file`/`append_file`/`edit_file` → path, write；`bash` → command, write；其他 → "", write（保守）
  - 依赖规则：同一 path 且至少一个 isWrite → 串行依赖边；不同 path → 可并发（即使都是 write）；bash 的 command 当 path 处理（不同 bash 命令可并发）
- **拓扑分组调度**（`topologicalGroups`）：
  - 按依赖关系分组：同一组的工具调用可并发执行；下一组必须等当前组全部完成
  - 组内顺序保持 LLM 原始顺序（结果回填顺序）
  - 单元素组直接串行执行（避免 spawn 开销）；多元素组 spawn 并发
- **Provider 连接预热**（`OpenAiProvider.warmup`）：
  - `OpenAiProvider` 构造时 `spawn { this.warmup() }`，发轻量 GET `/v1/models` 请求，触发 TLS 握手 + 连接池建立
  - 首次 `chatStream` 不再付 TLS 冷启动开销
  - 预热失败静默（预热是优化，不是必需）
- **性能基线测量**（`RunSummary` 新增 3 字段）：
  - `parallelBatches`：并发执行的批次数
  - `parallelSavedMs`：并发相比串行节省的毫秒数（`假设串行总耗时 - 并发实际耗时`）
  - `maxParallelism`：最大并发度（单组最多工具数）
  - spawn 分支记录每个工具的实际耗时，累加得假设串行耗时，对比并发实际耗时计算节省

**验证**

- 编译通过（`cjpm build success`）
- DAG 分组调度：同 path 写操作串行，不同 path 并发；无依赖 read-only 全并发
- Provider 预热：构造时 spawn 后台 `/v1/models` 请求，TLS 握手在 chatStream 前完成
- 性能基线：`RunSummary.parallelSavedMs` 记录并发节省的毫秒数

### 关键文件（本次会话新增/修改）

- `src/agent/loop.cj`：DAG 依赖分析（`extractResourceAccess`/`buildDependencyGraph`/`topologicalGroups`）+ 拓扑分组调度 + 性能基线计时
- `libs/cjllm/src/openai.cj`：`OpenAiProvider.warmup()` 连接预热 + 构造时 spawn 预热
- `src/main.cj`：`loadMcpServers` spawn 隔离 + 超时 catch（修复 `/ws` 404 根因）
- `docs/进度记录.md`：更新总览 + 新增本次会话记录

### 设计决策记录

- **DAG 依赖分析粒度**：文件资源级别（path + isWrite），不是工具级别。两个 `write_file` 操作不同文件可并发，操作同一文件串行
- **bash 工具的依赖处理**：`bash` 的 `command` 当 path 处理，不同 bash 命令可并发；相同 bash 命令串行（保守：bash 可能改任意状态）
- **拓扑分组 vs 全 DAG 调度**：选择拓扑分组（组内全并发，组间全串行），而非全 DAG 调度（每个节点独立等依赖完成）。拓扑分组实现简单，且对 LLM 并行工具调用场景足够（同批工具要么全独立要么有顺序依赖）
- **Provider 预热范围**：仅 `OpenAiProvider`（覆盖 DeepSeek/OpenAI/通义/Moonshot 主要场景）；Anthropic/Ollama 暂未加预热（Anthropic 端点不同，Ollama 本地无 TLS）
- **性能基线测量方式**：spawn 分支记录每个工具的实际耗时（`DateTime.now()` 前后差），累加得假设串行总耗时，对比并发实际耗时计算节省。`parallelSavedMs = serialMs - parallelMs`

---

## 当前状态：Web 支持实现方案 Step 1-5 已完成，待提交推送

### 本次会话（2026-08-26，第七轮）完成事项
> 主题：Web 支持实现方案 Step 1-5 + CangjieSkills 项目级 skill 安装

**Step 1：HTTP server 骨架**（`src/web/server.cj`）

- `WebServer`：`ServerBuilder` 链式构建 + `addr`/`port`/`distributor`/`maxRequestBodySize` 配置
- `WebDistributor <: HttpRequestDistributor`：实现 `distribute(path)` + 两个 `register` 重载（接口契约 3 方法全实现）
- `WebHandler <: HttpRequestHandler`：统一处理 `/`、`/assets/app.js`、`/assets/styles.css`、`/api/health`、404
- 静态资源内嵌（`static_assets.cj`）：`indexHtml`/`appJs`/`stylesCss` 三段式，6 套主题 CSS 变量 + 响应式布局
- 启动 banner：监听地址 + 鉴权状态 + 浏览器访问提示

**Step 2：WebSocket 流式对话**（`src/web/ws_handler.cj` + `protocol.cj` + `session_mgr.cj`）

- `WsHandler <: HttpRequestHandler`：`WebSocket.upgradeFromServer(ctx)` 升级 HTTP → WS
- 消息协议（`protocol.cj`）：`ChatRequest`/`ChatDelta`/`ChatDone`/`ChatError` JSON 帧
- 会话管理（`session_mgr.cj`）：`SessionStore` 落盘 `~/.cjh/sessions/s<timestamp>.jsonl`
- 前端 `appJs` WebSocket 连接管理：`connectWebSocket`/`sendMessage`/`handleServerMessage`/`appendDelta`/`handleDone`

**Step 3：REST API**（`src/web/rest_api.cj`）

- `RestApi <: HttpRequestHandler`：统一处理 `/api/*` 路由
- 端点：`GET /api/health`（健康检查 JSON）、`GET /api/sessions`（历史会话列表）、`GET /api/models`（可用模型列表）、`GET /api/tasks`（当前任务列表）
- 未知路由 → 404 JSON

**Step 4：前端 Markdown 渲染 + 代码块复制按钮**（`src/web/static_assets.cj`）

- CDN 引入：`marked.js`（Markdown 解析）+ `DOMPurify`（XSS 防护）+ `highlight.js`（代码高亮）
- `renderMarkdown(text)`：优先 marked + DOMPurify，离线降级为纯文本转义
- `enhanceCodeBlocks(container)`：给 `<pre><code>` 块注入复制按钮，hover 显示，`navigator.clipboard` + `execCommand` 降级
- 复制按钮 CSS：绝对定位右上角，hover opacity 1

**Step 5（可选）：auth_token 鉴权中间件 + 启动安全审计日志**（`src/web/auth.cj`）

- `Auth.check(ctx, config)`：`webAuthToken` 非空时校验 `Authorization: Bearer <token>`，失败写 401 + `WWW-Authenticate: Bearer`
- WebSocket 降级：从 URL 查询参数 `?token=...` 提取
- `SecurityAudit.logStartup(config)`：启动时打印绑定地址 + 访问范围（本机/远程）+ 鉴权状态 + TLS 状态
- 远程访问未启用鉴权时打印风险提示 + 建议 `--token <secret>`
- **TLS 跳过**：`ServerBuilder.tlsConfig` API 待确认，#9 标记可选

**CangjieSkills 项目级 skill 安装**

- git clone `atomgit.com/Cangjie-SIG/CangjieSkills` → 执行 `build.py` 构建知识库 `knowledge.sqlite3`（8.9 MB）
- 安装到项目级 skill 标准位置 `.atomcode/skills/cangjie-coding/`——atomcode 进 cjh 项目自动加载
- 不污染主 repo（`.atomcode/` 已在 `.gitignore`）
- 验证可用：`search_docs.py --query "HttpRequestDistributor"` / `"ServerBuilder"` 正常返回 API 契约
- 后续会话查 stdx.net.http 扩展标准库 API 直接用此 skill，不再跨会话遗忘

**验证**

- 编译通过（`cjpm build success`）
- `WebHandler` 单元路由：`/`→200（index.html）、`/assets/app.js`→200、`/assets/styles.css`→200、`/api/health`→200（JSON）、未知→404
- `RestApi` 路由：`/api/health`/`/api/sessions`/`/api/models`/`/api/tasks` 全部 200
- 启动安全审计日志正常打印
- `auth_token` 未配置时鉴权跳过（本机访问模式）

**已知问题（待修）**

- ⚠️ `/ws` 端点返回 404：`WebDistributor.distribute` 被框架调用（`/`/`/api/health` 返回 200 证明），但 `/ws` 路由查找失败。已尝试 HashMap 路由、直接 `match` 路由、指向 `WebHandler` 替代 `WsHandler`，均 404。需对照 `wv-sdk` 的 `StaticDistributor` 运行时行为进一步定位
- ⚠️ Web 端到端浏览器联调未完成（依赖 `/ws` 修复）

### 关键文件（本次会话新增/修改）

- `src/web/server.cj`（新）：`WebServer` + `WebDistributor` + `WebHandler` + 静态资源内嵌
- `src/web/ws_handler.cj`（新）：`WsHandler` WebSocket 升级 + 消息循环
- `src/web/protocol.cj`（新）：`ChatRequest`/`ChatDelta`/`ChatDone`/`ChatError` 消息协议
- `src/web/session_mgr.cj`（新）：`SessionStore` 会话落盘管理
- `src/web/rest_api.cj`（新）：`RestApi` REST API 端点处理
- `src/web/auth.cj`（新）：`Auth` 鉴权中间件 + `SecurityAudit` 启动审计日志
- `src/web/static_assets.cj`（新）：`indexHtml`/`appJs`/`stylesCss` 静态资源 + Markdown 渲染 + 复制按钮
- `src/main.cj`：`web` 子命令入口
- `libs/cjcfg/src/config.cj`：`CjhConfig` 新增 web 相关配置字段
- `docs/Web支持实现方案.md`（新）：Web 支持实现方案设计文档
- `.atomcode/skills/cangjie-coding/`（新，gitignore）：CangjieSkills 项目级 skill 安装

### 设计决策记录

- **`HttpRequestDistributor` 接口契约**：3 方法（`distribute(path)` + 两个 `register` 重载），全实现。CangjieSkills 确认结构正确
- **路由分发策略**：`WebDistributor.distribute` 用直接 `match` 语句分发，避免 HashMap 在 5+ 路由时的注册问题
- **静态资源内嵌 vs 外部文件**：选择内嵌（`static_assets.cj` 三段式字符串），单二进制部署，无需额外资源目录
- **Markdown 渲染 CDN 依赖**：marked.js + DOMPurify + highlight.js，离线降级为纯文本转义
- **auth_token 鉴权范围**：HTTP 请求 + WebSocket 连接（`?token=` 查询参数降级）
- **TLS 跳过原因**：`ServerBuilder.tlsConfig` API 待确认，Step 5 标记可选，TLS 暂缓

---

**V2b Step 1：plugin.json + Shell 工具插件**

- `src/tools/plugin.cj`（新文件）：
  - `PluginTool <: CjhTool`：shell 脚本执行，参数通过 `CJH_TOOL_ARGS` 环境变量传递（JSON），stdout 输出结果
  - `PluginManager`：扫描 `~/.cjh/plugins/*/plugin.json`，路径遍历防护，白名单过滤，注册 `PluginTool` 到 `ToolRegistry`
- `libs/cjcfg/src/config.cj`：`CjhConfig` 新增 `pluginsDir`/`enabledPlugins`；`ensureDirs` 创建 `pluginsDir`；`loadSettings` 加载 `enabled_plugins`
- `src/main.cj`：`loadPlugins()` 函数 + 三入口点（runCli/runJson/runTui）调用 + `agent.setPluginManager(pluginManager)` 接线
- 示例插件：`example/plugins/echo-test`（工具插件）+ `log-pruner`（事件钩子插件）

**V2b Step 2：事件钩子**

- `src/agent/loop.cj`：`Agent` 新增 `pluginManager` 字段 + `setPluginManager()` + `fireToolResultHook()` 方法
- 两处工具结果回填点（串行 + 并发执行路径）插入 `fireToolResultHook(tc.name, result.content)` 调用
- `on_tool_result` 钩子：工具结果回填前触发，插件可拦截/改写；事件数据通过 `CJH_HOOK_DATA` 环境变量传递

**V2b MCP 扩展点：MCP 协议支持**

- `src/tools/mcp.cj`（新文件）：
  - `McpClient`：stdio 传输 + JSON-RPC 2.0 + `initialize` 握手 + `tools/list` 查询 + `tools/call` 调用
  - `McpTool <: CjhTool`：代理转发到 MCP 服务器，`execute(args)` → `client.callTool(name, args)`
  - `McpManager`：管理多个 MCP 服务器的连接和工具注册，`connectAll()` + `disconnectAll()`
- `libs/cjcfg/src/config.cj`：新增 `McpServerConfig` 数据类（name/transport/command/args/url/env）；`CjhConfig` 新增 `mcpServers` 字段；`loadSettings` 加载 `mcp_servers` 段
- `src/main.cj`：`loadMcpServers()` 函数 + 三入口点调用
- 示例 MCP 服务器：`example/mcp/echo-mcp-server.sh`（bash 实现的最小 stdio MCP 服务器）
- 配置示例（`settings.json`）：
  ```json
  "mcp_servers": {
    "echo-mcp": {
      "transport": "stdio",
      "command": "bash",
      "args": ["/path/to/echo-mcp-server.sh"]
    }
  }
  ```

**验证**：
- echo-test 插件端到端测试通过：`▶ echo message: hello from plugin` → `↳ echo: [echo plugin] message: hello from plugin`
- MCP echo 服务器端到端测试通过：LLM 调用 echo 工具，MCP 服务器返回 `[echo plugin] message: hello from mcp`
- 编译通过（`cjpm build success`）

### 关键文件（本次会话新增/修改）
- `src/tools/plugin.cj`（新）：`PluginTool` + `PluginManager`
- `src/tools/mcp.cj`（新）：`McpClient` + `McpTool` + `McpManager`
- `libs/cjcfg/src/config.cj`：`McpServerConfig` + `CjhConfig` 新增 `pluginsDir`/`enabledPlugins`/`mcpServers`
- `src/agent/loop.cj`：`Agent` 新增 `pluginManager` + `setPluginManager()` + `fireToolResultHook()`
- `src/main.cj`：`loadPlugins()` + `loadMcpServers()` + 三入口点接线
- `example/plugins/echo-test/`（新）：示例工具插件
- `example/plugins/log-pruner/`（新）：示例事件钩子插件
- `example/mcp/echo-mcp-server.sh`（新）：示例 MCP 服务器
- `docs/插件系统实现方案.md`（新）：V2b 插件系统设计文档
- `docs/cjh功能清单.md`：更新到 v1.2.3，记录插件系统 + MCP 支持

---

### 本次会话（2026-08-25，第五轮）完成事项
> 主题：修复长 completions 场景下 token 统计为 0 的问题 + SSE 空闲超时

**问题现象**：生成 H5 游戏等长任务时，状态行一直显示 `↑ 0 tokens`，且偶发 `IllegalArgumentException: Invalid utf8 byte sequence`。

**根因定位**（通过加调试日志 + 真实长任务复现）：

1. **`readTimeout(60s)` 超时切断 SSE 流**（`openai.cj`）
   - DeepSeek 长 completions 60 秒后触发 `HttpTimeoutException: Read body timeout`
   - 异常被 `SseParser.run()` 的 catch 捕获 → `onError` → **带数值的最后一帧 usage 丢失** → token=0

2. **`usage: null` 帧不更新 token**（预期行为，非 bug）
   - DeepSeek 流式响应每一帧都带 `usage` 字段，但绝大多数是 `null`（`JsonKind.JsNull`）
   - 只有最后一帧（`stream_options.include_usage=true`）的 `usage` 才是 `JsObject` 且带数值
   - `match (u.kind())` 落到 `case _ => ()`，token 不更新——这是正确的

**修复**：

1. **SSE 空闲超时机制**（`sse.cj` + `openai.cj` + `message.cj` + `loop.cj`）
   - `readTimeout(60s)` 是**每次 read 的空闲超时**（非绝对超时），实测长任务 chunk 持续到达不会触发
   - `SseParser` 新增 `timedOut: Bool` 标志 + `HttpTimeoutException` 专用 catch
   - timeout 时 `flushRemaining()` 保留已收数据 + `onDone` + 标记 `timedOut`
   - `ModelResponse` 新增 `timedOut: Bool` 字段
   - `OpenAiProvider` 把 `parser.timedOut` 传到 `ModelResponse`
   - `agent/loop.cj` 检查 `response.timedOut`，给用户显示提示 + 给 LLM 注入 `[System Notice]`

2. **`StreamAccumulator.feed` usage 解析健壮性**（`provider.cj`）
   - 原来只处理 `JsInt`，遇到 `JsFloat`/`JsString` 会静默丢弃 token 用量
   - 新增 `extractNum` 闭包：统一支持 `JsInt`/`JsFloat`/`JsString` 三种形态
   - `prompt_tokens`/`completion_tokens`/`prompt_cache_hit_tokens`/`cache_read_input_tokens` 全部走 `extractNum`
   - 注意：仓颉 `stdx.encoding.json.JsonKind` 只有 7 个成员（`JsObject`/`JsArray`/`JsInt`/`JsFloat`/`JsString`/`JsBool`/`JsNull`），**没有 `JsInt64`**；`JsonValue` 也没有 `asInt64()`。`JsonInt.getValue()` 本就返回 `Int64`，已覆盖整数场景

3. **`resetAgentTimer` 每轮重置计时**（`main.cj`/`app.cj`）
   - `onThinking` 回调里调 `tui.resetAgentTimer()`，让 Agent 状态行耗时从每轮开始计算

**验证**：
- 编译通过（`cjpm build success`）
- 短任务（1+1=）：1.3s · 2.2K tokens · 99% cached，正常
- 真实长任务（生成 H5 我的世界游戏）：10 rounds · 10 tools · 112.9s · 133K tokens · 99% cached
- token 持续累加：`54888 → 69307 → 85071 → 101811 → 118751`
- **无 timeout、无 error** —— `readTimeout(60s)` 空闲超时没有误切断长任务
- usage 解析单元测试：JsInt/JsFloat/JsString 三种形态 15/15 全过

### 关键文件（本次会话修改）
- `libs/cjllm/src/openai.cj`：`readTimeout(60s)` 空闲超时 + 传 `timedOut` 到 `ModelResponse`
- `libs/cjllm/src/sse.cj`：新增 `timedOut` 标志 + `HttpTimeoutException` catch 保留已收数据
- `libs/cjllm/src/provider.cj`：`extractNum` 闭包统一处理 JsInt/JsFloat/JsString
- `libs/cjllm/src/message.cj`：`ModelResponse` 新增 `timedOut: Bool` 字段
- `libs/cjllm/src/llm_test.cj`：新增 4 个 usage 解析测试用例
- `src/agent/loop.cj`：检查 `response.timedOut`，显示用户提示 + 注入 `[System Notice]`
- `src/main.cj`：`onThinking` 回调调 `resetAgentTimer`
- `src/tui/app.cj`：新增 `resetAgentTimer()` 公开方法

### 设计决策记录
- **`readTimeout` 语义**：文档说「读取一个响应的最大时长」，但实测是**每次 read 的空闲超时**。长任务 chunk 持续到达，60s 空闲超时不会触发；网络卡住 60s 无新数据才断
- **三个 Provider 的覆盖情况**：
  - OpenAI/DeepSeek/通义/Moonshot：✅ 用 `SseParser`，`readTimeout(60s)` 空闲超时生效
  - Ollama：✅ 复用 `SseParser`，`sse.cj` 的 timeout catch 对它生效（Ollama 没设 readTimeout，走默认）
  - Anthropic：✅ 复用 `SseParser`，同上（Anthropic 也没设 readTimeout）
- **Ollama/Anthropic 不显式设 readTimeout**：它们原本没设，走默认无超时。这次改动只修了 OpenAI 路径，Ollama/Anthropic 的 `SseParser` timeout catch 是「保险绳」而非「日常路径」

---

## 当前状态：主题系统扩展 + 实时预览已推送，origin main 同步至最新

### 本次会话（2026-08-24，第四轮）完成事项
> 主题：TUI 主题系统扩展 + 切换实时预览

1. **新增 4 套经典主题**（`theme.cj`）：
   - 参考 oh-my-pi (omp) TUI 主题配色方案，omp 内置 80+ 套主题（catppuccin/dracula/gruvbox/nord/tokyo-night/rose-pine 等），用 hex 配色 + vars 变量引用
   - cjh 用 256 色索引近似 omp hex 配色，新增 4 套：
     - **dracula**：紫粉青绿经典暗色（accent=紫 #BD93F9）
     - **nord**：北欧极简冷色冰原（accent=冰蓝青 #88C0D0）
     - **gruvbox**：复古暖色橙黄绿棕（accent=橙 #FE8019）
     - **tokyo-night**：东京夜色深蓝紫粉霓虹（accent=紫 #BB9AF7）
   - 每套覆盖 primary/accent/purple/ok/warn/err/muted/info/highlight/text/bg 全部 Palette 字段
   - `/theme <name>` 运行时切换，`Theme.list()` 列出全部 6 套主题

2. **主题切换实时预览**（`app.cj`/`main.cj`）：
   - **需求**：`/theme` 无参数弹出 picker 时，方向键移动即时预览主题效果，Enter 确认持久化，Esc 取消恢复原主题
   - **实现**：
     - `showThemePicker` 记录原主题 `themePreviewOriginal`
     - picker 方向键（`isArrowUp`/`isArrowDown`）移动时调用 `Theme.apply(picker.current())` 即时预览
     - Enter 确认：`Theme.apply(cur)` + `onThemeApply(cur)` 回调持久化
     - Esc 取消：`Theme.apply(themePreviewOriginal)` 恢复原主题
   - **新增回调**：`TuiApp.onThemeApply: (String) -> Unit`，`main.cj` 接线写回 `settings.json`（`cjhCfg.saveTheme`）

3. **磁盘空间清理**：
   - 编译时报 `No space left on device`（50G 磁盘 100% 满）
   - 清理 `/root/ohos-sdk`（4G）/ `/root/ohos_vcpkg`（1.1G）/ `/root/.cache/ms-playwright`（1.2G）
   - 腾出 4.5G 空间，编译恢复正常

### 关键文件（本次会话新增/修改）
- `libs/cjterm/src/theme.cj`：新增 4 套主题（dracula/nord/gruvbox/tokyo-night）
- `src/tui/app.cj`：主题实时预览（picker 方向键即时预览 + Esc 恢复 + Enter 持久化）
- `src/main.cj`：`onThemeApply` 回调接线（写回 settings.json）

### 待排查
- **UTF8 异常**：用户反馈偶发 `错误: IllegalArgumentException: Invalid utf8 byte sequence.`，已加日志堆栈重载（`Log.fatal(msg, e)`），下次复现时 `/tmp/cjh_error.log` 会记录 `fileName:lineNumber @ declaringClass` 行号定位

---

## 当前状态：动态 Agent 状态行 + 布局修复 + 日志堆栈已推送，origin main 同步至 `d553a32`

### 本次会话（2026-08-24，第三轮）完成事项
> 主题：动态 Agent 状态行 + TUI 布局修复 + 日志堆栈增强

1. **动态 Agent 状态行**（commit `d553a32`）：
   - **新增回调**（`loop.cj`）：`onThinking`（chatStream 前="Thinking…"）、`onTokens`（累计输入 token）
   - **TUI 接线**（`app.cj`/`main.cj`）：`setAgentState`/`addAgentTokens` + `renderAgentStatus`
   - **配色**：⠏ spinner 青色（`THEME_ACCENT`）· 状态文字 绿色（`THEME_OK`）· 计时淡灰（`THEME_MUTED`）· token 数琥珀金（`THEME_WARN`）
   - **计时刷新**：主循环 agent 执行中时每 1 秒强制 `markDirty()` 刷新动态状态行计时（按秒递增）
   - **格式**：`⠏ Thinking… (5s · ↑ 102 tokens)` / `⠏ Running bash…` / `⠏ Streaming…`

2. **修复回复滚动覆盖动态状态行**（commit `d553a32`）：
   - **根因**：输出区高度 `outH = statusRow-8` 占满到 `statusRow-1`，动态状态行 `agentRow` 也在此范围内，长回复滚动时 `outputView.render` 的 `setLine` 覆盖动态状态行
   - **修复**：agent 执行中时输出区底部从 `statusRow-1` 收缩到 `agentRow-1`（动态状态行上方，间隔1行）
   - **依赖处理**：`TasksPanel` 新增 `height(outH)` 预计算方法，在输出区高度计算前拿到 `tasksH`，用 `outH0` 限制避免循环依赖

3. **修复总结条后状态栏消失**（commit `d553a32`）：
   - **根因**：`onRunComplete` 在 `agent.run` 内部触发（loop.cj:345），此时 `agentRunning=true`，输出区已收缩到 `agentRow-1`。总结条通过 `TuiBridge.output` 写入收缩后的输出区，被挤压。然后 `setAgentRunning(false)` 执行，动态状态栏消失
   - **修复**：让 `agent.run` 返回 `RunSummary`（新增 `reply` 字段），spawn 块在 `setAgentRunning(false)` **之后**才触发 `onRunComplete`。这样动态状态栏先消失、输出区恢复占满，总结条正常显示在输出区末尾
   - **同步更新**：`--verify` 模式（`main.cj:73`）和 `runJson`（`main.cj:859`）的调用点改为 `RunSummary`

4. **日志增强：FATAL/ERROR 带异常堆栈**（commit `d553a32`）：
   - **新增重载**：`Log.fatal(msg, e)` / `Log.error(msg, e)`，利用仓颉 `Exception.getStackTrace()` 记录 `fileName:lineNumber @ declaringClass`，最多 20 帧
   - **实现**（`logger.cj`）：`stackTraceOf(e)` 提取堆栈逐行拼接
   - **接线**：`runTui` 启动失败的 FATAL 捕获点改用新重载（`main.cj:1161`）
   - **排查能力**：下次触发 `IllegalArgumentException: Invalid utf8 byte sequence` 时，`/tmp/cjh_error.log` 会记录完整堆栈，精确定位异常源头

5. **SSE 读超时**（commit `d553a32`，`openai.cj`）：
   - 流式响应读超时设 5 分钟（`readTimeout(Duration.second * 60)`），避免 DeepSeek 长 completions 触发 WARN read response timeout

### 待排查
- **UTF8 异常**：用户反馈偶发 `错误: IllegalArgumentException: Invalid utf8 byte sequence.`，已加日志堆栈重载，下次复现时 `/tmp/cjh_error.log` 会记录行号定位

---

## 当前状态：v1.2.2 已发布并推送，origin main 同步至 `162ad99`

### 本次会话（2026-08-24）完成事项
1. **输入队列方案 B**（commit `45f0f36`）：Agent 执行期间输入框仍可编辑，提交入队 + 提示「已入队，将在当前任务完成后发送（前面还有 N 条）」，执行完 `dequeueInput` 自动处理下一条
2. **Tasks 面板**（commit `45f0f36`）：`TodoItem`/`TodoStatus` 数据层 + `TasksPanel` UI 组件（对齐 Claude Code 样式）+ `onTodosChanged` 回调实时刷新
3. **TodoWriteTool**（commit `45f0f36`）：LLM 通过 `todo_write` 工具自动管理任务（add/doing/done/update/clear/list）。**高内聚低耦合设计**：回调注入避免 `cjh.tools ↔ cjh.agent` 循环依赖
4. **UI 文案优化**（commit `45f0f36`）：「Agent made by Cangjie」→「Harness made by Cangjie」（7 处）
5. **v1.2.1 发布**（commit `8cc8b49`）：版本号小更新递增
6. **状态栏边框改蓝色醒目**（commit `f9d9ed8`）：`+--`/`->`/`+`/横线填充全部 `COLOR_BLUE`（39）
7. **星霜青主题系统**（commit `71adf31`）：`Theme` 单例 + `Palette` 色板类 + `apply(name)` 写回 `Ansi.THEME_*`，2 套内置主题（starfrost 默认 / classic 向后兼容）。`CjhConfig.theme` 字段读 `settings.json`，`saveTheme()` 持久化。`/theme [name]` 命令运行时切换。所有硬编码 `COLOR_*` 替换为 `THEME_*` 常量
8. **/theme 交互式 picker bug 修复**（commit `4664ab9`）：`/theme` 无参数时弹出 `ListPicker`，上下键选择，Enter 确认，一步到位切换主题
9. **回复 UI 打磨**（commit `d1fb473`/`4fea476`/`56afcec`）：回复左侧缩进 1 空格 + `❯` 后空行 + 回复后空行 + 去横线 + 用户输入前空行
10. **回合总结条功能**（commit `c3cff45`）：每轮 Agent.run 结束后渲染 `─── ✓ 2 rounds · 3 tools · 42.6s · 1.53K tokens · 99% cached ───`。`ModelResponse.usage` 新增 `TokenUsage` 字段，`StreamAccumulator.feed` 解析 OpenAI/DeepSeek/Anthropic 的 usage 字段，`RunSummary` 类收集统计，`onRunComplete` 回调渲染总结条
11. **cjh 功能清单文档**（commit `8a67e12`）：`docs/cjh功能清单.md` 完整列出所有已具备和支持的功能
12. **v1.2.2 发布**（commit `162ad99`）：版本号小更新递增，回合总结条上下加空行，推送 `cj-env.sh`

### 真实 LLM 端到端验证状态
- ✅ DeepSeek via taotoken（`https://taotoken.net/api/v1`，model `deepseek-v4-flash`）
- ✅ LLM 成功调用 `todo_write` 工具（add×3 + doing×1 + list×1）
- ✅ 回合总结条正确显示 token 用量和缓存命中率
- ✅ 主题切换 `/theme starfrost`/`/theme classic` 正常工作

### 关键文件（本次会话新增/修改）
- `libs/cjterm/src/theme.cj`（新）：`Theme` 单例 + `Palette` 色板 + 2 套内置主题
- `libs/cjterm/src/taskspanel.cj`（新）：`TasksPanel` UI 组件
- `src/tools/todowrite.cj`（新）：`TodoWriteTool`（回调注入，避免循环依赖）
- `libs/cjllm/src/message.cj`：`ModelResponse.usage` + `TokenUsage` 类
- `libs/cjllm/src/provider.cj`：`StreamAccumulator` 解析 usage 字段
- `src/agent/loop.cj`：`RunSummary` 类 + `onRunComplete` 回调 + `Agent.run` 收集统计
- `src/tui/app.cj`：`isAgentRunning` + 输入队列 + `showThemePicker` + 空行分隔
- `src/main.cj`：`/theme`/`/task` 命令 + `onRunComplete` 渲染回合总结条
- `libs/cjterm/src/ansi.cj`：`THEME_*` 从 `static let` 改 `static var`，星霜青配色
- `docs/cjh功能清单.md`（新）：功能清单文档
- `docs/cjh-TUI主题设计方案.md`（新）：TUI 主题设计方案文档
- `cj-env.sh`（新）：仓颉工具链环境脚本

## 当前状态：v1.2.3 已发布并推送，origin main 同步至 `081c1c1`

### 本次会话（2026-08-24，第二轮）完成事项
> 主题：OMP 式写入架构 + 英文提示词 + Hashline 内容校验

1. **OMP 式写入架构**（commit `bb68c86`）：三层防线对齐 OMP/Aider/Cursor
   - **工具契约层**：`AppendFileTool` 新增（增量追加）；`WriteTool` 加 content 软上限（150 行/4000 字符）超限警告但不阻断
   - **截断打断层**：`ModelResponse.truncatedPaths` + `StreamAccumulator` 记录被截断 write_file/edit_file 的 path → agent loop 注入 `[System Notice]` 绝对指令（已落盘、禁止重写、强制 append_file 续写到闭合），斩断"重写 → 截断 → 重写"死循环
   - **前缀冻结层**：messages 数组审计确认 append-only；`truncateToolResult` Tool Output 压缩 Hook（>6000 字符 Head/Tail 裁剪）
2. **英文提示词改造**（commit `bb68c86`）：
   - 所有工具 schema description 改英文（bash/read_file/write_file/append_file/grep/list_dir/hashline_edit）
   - 默认 systemPrompt「大文件写入策略」段改英文（保留 "answer in Chinese" 让模型回复中文）
   - 截断打断注入的 `[System Notice]` 绝对指令改英文（并发 + 串行两个分支）
3. **v1.2.3 发布**（commit `bb68c86`）：版本号小更新递增
4. **Hashline 行号锚点可选内容校验**（commit `081c1c1`）：
   - `PatchOp.LineAnchor(Int64)` → `LineAnchor(Int64, String)`，第二参数是可选 expected content
   - `parseLineAnchor` 返回 `Option<(Int64, String)>`，按首个空格分隔行号和可选 content
   - `apply` 的 `LineAnchor` 分支：expected 非空时校验 `lines[lineIdx]==expected`，不匹配拒绝
   - spec description 文档化 `@@N content` 可选校验 + content guard 示例

### 验证状态
- ✅ `cjpm build` success
- ✅ `cjpm test` 0 failed
- ✅ H5 射击小游戏端到端验证：文件完整闭合（`</script></body></html>` 齐全），3022 字节，未触发截断死循环

### 本次会话关键文件
- `src/tools/builtin.cj`：`AppendFileTool` 新增 + `WriteTool` 软上限
- `src/tools/hashline.cj`：`PatchOp.LineAnchor` 加可选内容校验
- `libs/cjcfg/src/config.cj`：默认 systemPrompt 英文化
- `libs/cjllm/src/message.cj`：`ModelResponse.truncatedPaths` 字段
- `libs/cjllm/src/provider.cj`：`StreamAccumulator.truncatedPaths` + finish() 透传
- `src/agent/loop.cj`：截断打断 + `truncateToolResult` Tool Output 压缩 Hook
- `src/main.cj`：5 处 registry 注册 `AppendFileTool()`

### 版本号规则（重要！）
- **重大更新**：中间位递增（v1.2.0 → v1.3.0）
- **小更新**：最后一位递增（v1.2.0 → v1.2.1）
- 发版时同步更新：`cjpm.toml` version + `libs/cjterm/src/logo.cj` `Logo.VERSION` + 标题栏注释，打 tag 并推送

### 代码组织原则（重要！）
- **高内聚低耦合**：满足软件设计六大原则（单一职责、开闭、里氏替换、接口隔离、依赖倒置、迪米特法则）
- **单向依赖 + 回调注入**：循环依赖靠单向依赖 + 回调注入解决，禁止"移包打补丁"破坏包内聚性
- 例：`TodoWriteTool` 留在 `cjh.tools` 包，通过函数回调操作 `Agent` 任务列表，避免 `cjh.tools ↔ cjh.agent` 循环

### 下一步可推进
- V2e IM 网关（Channel 抽象 + Web 渠道 + 审批远程化）
- V3 信任链/协议深化
- V4 多 agent + 鸿蒙
- P3-P4 工具效率（LSP 语义高亮 + DAP 调试栈展示）

---

## 一、已完成里程碑

### 核心功能
1. **P0 MVP**：agent 核心（消息状态机+工具协议+迭代）、5 工具（bash/read_file/write_file/grep/list_dir）、
   OpenAI 兼容 + Anthropic 双协议（SSE 流式+工具分片累积）、mock 离线验证、TUI、CLI、--mock —— 全端到端验证
2. **P1 配置系统**：`~/.cjh/`（settings.json + auth.json + sessions/ + CJH_CONFIG_DIR 覆盖）、
   env 覆盖、无 key 提示 auth.json 路径、**首次自动生成模板**
3. **P1 会话持久化**：JSON 消息历史（全角色）、--list / --resume、紧凑会话 ID、CLI 也支持 --cli --resume
4. **P1 剩余**：SSE 流式实时上屏（takeDelta 增量）、Anthropic 协议、grep/list_dir、4 用例单测
5. **真实 DeepSeek 验证通过**：工具调用链（read_file→grep→list_dir→答复）、中文对话

### 架构与库
6. **三库拆分**：cjterm（终端 UI，纯 libc FFI 零依赖）+ cjllm（LLM 客户端）+ cjcfg（配置/会话）
7. **v2 架构文档**（docs/方案与架构设计-v2.md）：
   - 立项初衷（用户原话）+ 仓颉独有优势严格定义 + 借力而非排斥的评审标准
   - 三域 capability 安全模型、插件系统（cjpm 包+签名信任链）、记忆分层、
     并发执行引擎（工具 DAG）、IM 渠道网关（微信/钉钉远程协同）
8. **模块规划**（docs/模块规划.md）：拆分原则、候选库、操作清单

### 关键修复（踩坑沉淀）
9. **RPATH**：产物内嵌 DT_RPATH（--disable-new-dtags），./main 无需 LD_LIBRARY_PATH
10. **TLS**：ClientBuilder 需 tlsConfig(TlsClientConfig())，修复 "TLS must be configured"
11. **流式工具调用**：delta.tool_calls（在 choices[0].delta 内非顶层）
12. **base_url 自动补全**：OpenAI /v1/chat/completions、Anthropic /v1/messages
13. **provider 劫持**：只有显式 CJH_PROVIDER=anthropic 才用 Anthropic（不再被 env 劫持）
14. **中文 mojibake 根治**（多轮）：
    - 迭代 String 得 Byte（非码点）→ truncateLine/mock 不用 Rune(byte) 重建
    - takeDelta 字节切片 → 全文比对（lastSentText）
    - **DeepSeek 服务端自身返回 mojibake**（"你好"→"ä½\xa0å¥½"）→ MojibakeFix
      解码修复；hasCjk 判定曾用 Byte 迭代恒 false → 改 toRuneArray
    - **流式分片切碎 mojibake 根治**：feed() 只累积原始文本，finish() 对完整串
      整体 MojibakeFix.fix（逐 chunk 修复在分片切碎乱码串时必失败）；
      TUI 新增 streamStartPos + endStream() 用修复后完整文本整体重绘替换流式段
      （onAssistantText 改为 endStream(md.process(text)+md.finish())）
15. **中文 IME 输入**：readKey 支持多字节 UTF-8，handleKey 接受任意字符
16. **中文退格崩溃**：Backspace 按字符删除（字节安全回退）
17. **输入焦点丢失**：appendDelta 跨线程直接 render 竞态 → 只 markDirty + renderLock

### UI 打磨
18. **logo**：box-drawing CJ → ASCII art "CJ"（C 蓝+J 青+3 空格分隔，消除 CI 误读）
19. **布局**：logo 区 → 标题栏（含模型徽章）→ 分隔线 → 输出区 → 状态徽章 → ❯ 输入行
20. **配色**：回复默认白、工具调用绿、参数灰、错误红、输入提示亮青（参考 omp）
21. **markdown 高亮**：MarkdownRender 流式渲染（**粗体**/`行内代码`/```代码块```）
22. **会话分隔**：每轮回复后淡色分隔线；用户消息 ❯ 前缀（去 cjh>）
23. **stderr 屏蔽**：Stderr.suppress/restore，TUI 全屏模式不污染屏幕
24. **TLS unknown CA 修复**：
    - 现象：真实请求报 `certificate verify failed (134), TLS alert unknown CA(F)`
    - 根因①：stdx `TlsClientConfig()` 默认不自动发 SNI，网关（taotoken.net）
      在无 SNI 时返回不可信证书 → 显式 `tlsConfig.domain = Some(sniHost())`
    - 根因②：`endpointUrl()` 补全 bug：base 以 `/v1` 结尾时不追加
      `/chat/completions`，请求打到裸 `/api/v1` 返回 401 → 已修复补全逻辑
    - 附带：`verifyMode = CustomCA(X509Certificate.systemRootCerts())`
      显式加载系统根证书（默认 verifyMode 在部分环境不可用）
25. **鼠标事件**（commit a78ecb8）：
    - `readKey` 完整解析 CSI/SGR/X10 鼠标协议（ESC[<b;x;yM 读到终结字母、
      ESC[M+3 字节完整消费防残留）；KeyEvent 新增 isMouse/button/x/y
    - SS3 应用方向键（ESC O A/B/C/D）：alt-screen 下滚轮映射、部分终端默认，
      必须整组识别，否则被拆成 ESC/O/A 残留字母输入
26. **/ 命令下拉补全**（a78ecb8）：ListPicker 组件（滚动窗口+高亮，新文件 picker.cj）；
    输入 `/` 开头实时弹出候选（/help /new /resume /model /settings /quit），
    方向键选择、Tab/Enter 确认、裸 Esc 关闭；文件日志 CJH_TUI_LOG（默认 /tmp/cjh_tui.log）
27. **差分渲染**（a78ecb8）：逐行对比 prevLines 只重绘变化行（省 ANSI 输出）；
    sizeChanged() 首帧/resize 清缓存强制全量重绘
28. **logo box-drawing 版**（a78ecb8）：UTF-8 终端用 box-drawing CJ
    （J 顶钩 ██╗ + 底钩 ╚═╝，不再误读为 I），非 UTF-8 降级 ASCII（J 带左钩 |_/）
29. **不用 alt screen 的决策**（a78ecb8）：alt screen 下滚轮映射为 SS3 方向键
    （ESC O A/B 连续多次）时序拆分会残留字符 → 保持主屏幕 + clearScreen 全量重绘，
    滚轮恢复终端原生滚动行为
30. **TUI 组件化重构**（参考 oh-my-pi pi-tui 契约）：cjterm 新增
    `Component` 接口（render/handleKey/onFocus/onBlur）+ 四个可复用组件——
    `TuiCanvas`（行级差分画布，从 app.cj prevLines 抽出）、`InputBox`（❯ 提示 +
    UTF-8 退格 + onChange/onSubmit 回调）、`OutputView`（行集合 + 滚动 +
    流式段管理）、`StatusBar`（状态行 + 徽章回退）；`app.cj` 从 485 行瘦身到 363 行，
    只做布局编排 + 输入分发 + 线程安全包装，渲染细节全部下沉 cjterm 组件
30b. **TUI 组件扩展（Spinner + ConfirmDialog）**：
    - `Spinner`（cjterm/src/spinner.cj）：流式加载指示器，UTF-8 braille 转圈帧
      （⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏）/ 非 UTF-8 降级 |/-\，tick() 逐帧推进；接入
      app.cj：`outputView.isStreaming()` 时状态行显示（outputLock 保护读）
    - `ConfirmDialog`（cjterm/src/confirm.cj）：带边框模态确认弹窗（UTF-8
      box-drawing ╭╮╰╯，非 UTF-8 降级 +-|），y/Y=确认 n/N/Esc=取消（Enter 谨慎
      默认取消，enterAccepts 可改），onResult 回调；**取代 app.cj 内联审批 UI**
      （原状态行黄色审批提示 + handleKey 手写 y/n 判定 → 统一走组件模态消费 +
      回调写回 approvalResult，agent 线程 requestApproval 语义不变）
    - 对齐 omp pi-tui 组件清单：cjh 已有 input/scroll-view/select-list 等价物；
      loader（Spinner）与确认弹窗补齐；多行 editor/tab-bar/box 布局件留作后续
30c. **TUI 组件补齐（MultiLineEditor + TabBar + Box）**：
    - `MultiLineEditor`（cjterm/src/editor.cj）：多行文本编辑器——行缓冲区 +
      方向键光标移动（上下保持目标列）+ 可打印字符插入 / Enter 拆行 /
      Backspace 删除（行首合并上一行）/ Ctrl+D 删除（行尾合并下一行）/
      PageUp/PageDown 光标翻页 + 视口自动滚动保持光标可见；所有行内操作
      经 toRuneArray 逐码点重建（UTF-8 多字节安全）；行号栏弱化色；
      onChange 回调（参数为完整文本）
    - `TabBar`（cjterm/src/tabbar.cj）：标签栏——add/setTabs/setActive +
      左右方向键 / Tab 切换（循环回绕）+ 选中标签主题色底黑字高亮 +
      鼠标点击标签行命中选中（记录 lastRenderRow 坐标换算）；onSelect 回调
    - `Box`（cjterm/src/box.cj）：带边框布局容器——UTF-8 box-drawing
      （╭╮╰╯─│）/ 非 UTF-8 降级 ASCII；左上角标题（主题色加粗）；
      内嵌子组件（Option<Component>，注意 Cangjie 无 `T?` 可空类型语法，
      用 Option<T>）委托渲染于内容区（去边框 + 内边距），子组件未填满
      补空白内容行；容器为焦点边界（子组件不消费也不穿透）
    - 至此对齐 omp pi-tui 组件清单完整补齐：input/scroll-view/select-list/
      loader/confirm 已有，editor/tab-bar/box 本次落地；三组件均编译通过
      （cjpm build success，随 Spinner/ConfirmDialog 一起提交）
31. **V2a 三域安全（capability + 审批链）**：
    - **三域模型**（libs/cjcfg/src/capability.cj，`Capability` + `CapDecision`）：
      - 命令域 `commands`：bash 命令白名单（token 精确匹配 / `system*` 通配）
      - 工具域 `tools`：工具名白名单（未声明即拒绝）
      - 资源域 `resources`：`fs` 路径 glob 白名单 + `!` 排除、`net` 网络白名单（预留）、
        `proc` 危险命令黑名单（`!rm` 禁止）；手写 glob 引擎（`*` 不跨 `/`、`**` 递归、
        `?` 单字符、`/a/**` 匹配目录自身、`$HOME` 展开）
    - **最小权限**：settings.json 配置了 `capability` 字段即严格模式（未声明即拒绝）；
      未配置 = 宽松模式（全放行，向后兼容）
    - **审批链**：`approval` 字段（工具名 / `bash:write` 写类命令 / `proc:*` 全命令 /
      `*:*` 全工具）→ `CapDecision.NeedApproval`；TUI 内嵌审批 UI（状态行黄色
      `⚠ 审批: ... [y]=同意 [n]/[Esc]=拒绝`，主循环 y/n 写回，agent 线程
      `requestApproval()` 阻塞等待）；CLI 读 stdin 一行 y/n（EOF 默认拒绝）
    - **接入**：`Agent.executeGuarded()` 在工具执行前依次检查工具域/命令域/资源域，
      拒绝返回错误 ToolResult 回填 LLM，审批通过才执行；`AgentConfig` 传入 capability，
      `agent.onApproval` 回调由 TUI（TuiBridge.requestApproval）/CLI 接线
    - **验证**：cjcfg 6 个 capability 单测（宽松/工具白名单/命令白名单+黑名单/命令审批/
      路径 glob+排除+$HOME/glob 语义）全过；cjllm 9 用例回归通过；构建 0 错误；
      CLI mock 端到端演示拒绝路径（read_file 不在白名单 → "被安全策略拒绝"）与
      审批路径（approval `*:*` → "⚠ 需人工确认" + 拒绝分支 "操作被用户拒绝"）
32. **TUI truncate CSI off-by-one 致 status bar 宽度塌缩**（2026-08-23，隐藏极深）：
    - 现象：177 列终端，状态栏右侧填充线 `------` 只渲染十几列就截断，
      `displayWidth(sb)=176`（正确）但 `canvas.truncate(sb, cols)` 输出可见宽度 `truncW=81`
      —— 两个宽度函数对同一字符串算出 95 列差
    - 根因：`TuiCanvas.truncate()` 处理 `ESC[` CSI 序列时，进入 while 循环前
      **没有先消费 `[` 字节**。`[` = 0x5B 正好落在终结字母区间 `0x40..0x7E`，
      第一次循环就把 `[` 误判为终结符并 break → CSI 参数和真正终结字母（`m`）
      残留流中，被后续主循环当作可见字符计入宽度，可见宽度反而偏小
    - 对照：`displayWidth()` 的 CSI 处理是对的（先 `i += 1` 跳过 `[`），
      两个函数对 `[` 的处理不一致是 bug 温床
    - 修复（libs/cjterm/src/component.cj）：CSI 分支先 `sb.append('['); i += 1`
      再进参数/终结字母循环，与 `displayWidth()` 严格对齐
    - 教训：**两个等价宽度函数（displayWidth / truncate）必须共享同一段
      ANSI 解析逻辑**，否则一处改了一处忘改，差一个字节就是几十列误差。
      排查时同时打两个函数的宽度日志，对比同一字符串的输出，差异点即 bug
    - 附带清理：删除 component.cj 的 `diagLog` 函数和 3 处诊断埋点；
      statusbar.cj 删除临时 `_renderReal()` / `getHomeStr()` / 诊断行 /
      `import std.fs.*`；状态栏改 ASCII 边框（`+-- ... ->`、`# ` 模型前缀、
      `[ cwd ]` 目录段）规避 emoji/box-drawing 宽度歧义
33. **V2b 插件系统（Skills 即文件 + DeclarativeTool）**（2026-08-23）：
    - **Skills 即文件**（对齐 pi 的"技能即文件"）：`~/.cjh/skills/<name>.md`
      frontmatter（name/description/tools）+ 正文为指令；正文注入 system prompt
      （`buildSystemPrompt` = 全局 + AGENTS.md + Skills），工具段注册为
      DeclarativeTool；`enabled_skills` 白名单（空=全启用）
    - **parseFrontmatter**（src/main.cj）：统一解析 frontmatter，支持单行标量
      （`name: xxx`）和多行块（`tools:` 单独成行，后续缩进行合并为 JSON 数组
      字面量）；替换原 listSkills/loadSkills/loadSkillTools 三个函数里按行
      startsWith 的 bug 解析（多行 JSON 数组单行 t[6..] 只能拿到空串）
    - **DeclarativeTool**（src/tools/registry.cj）：shell 命令模板 +
      参数 schema 声明；execute 时 `${argName}` 占位符展开为参数值，
      `executeWithOutput("/bin/bash", ["-c", cmd])` 执行；frontmatter 的
      tools 段每项 `{name, description, command, args?}` 注册到 ToolRegistry
    - **/skills 命令**：扫描技能目录，输出名称、启用状态（●/○）、描述、
      声明式工具数（`countSkillTools` 解析 JSON 数组长度）
    - **验证**：示例技能 `~/.cjh/skills/example.md`（echo_hi + disk_usage
      两个声明式工具）；`cjpm build` 0 错误；PTY 实测 `/skills` 输出
      `● 启用 example — ... [2 个声明式工具]`
34. **V2c 记忆分层（Compaction + AGENTS.md）**（2026-08-23）：
    - **Compaction 自动压缩**（src/agent/loop.cj `maybeCompact`）：消息条数超
      `compactThreshold` 阈值时（0=禁用），用 LLM 把早期消息（索引 1..size-keep）
      压成中文摘要（保留任务目标/工具调用结果/当前进度/未决问题），
      重建 messages = system + 摘要 + 最近 `compactKeep` 条；摘要失败不阻塞主流程
    - **手动压缩 /compact**（loop.cj `compact(force)` + main.cj handleSlashCommand）：
      `Agent.compact(true)` 无视阈值强制压缩，返回 (是否压缩, before, after)；
      spawn 线程执行避免阻塞 TUI，输出 `[历史已压缩：N → M 条消息]`
    - **AGENTS.md 项目指令**（main.cj `loadProjectInstructions`）：从当前工作目录
      逐级向上查找 8 层，找到第一个 AGENTS.md 读取内容；`buildSystemPrompt`
      拼接 = 全局 systemPrompt + `## 项目指令（AGENTS.md）` + `## 可用技能（Skills）`
    - **接线**：runCli/runJson/runTui 三处都调 buildSystemPrompt；
      `/help` 列表补全 /compact /skills /tree /fork
    - **验证**：示例 AGENTS.md 在 /tmp/agents_test 下被正确加载；
      `cjpm build` 0 错误；PTY 实测 `/help` 含 `/compact`、`/compact` 触发压缩链路
35. **树形会话 + --mode json**（2026-08-23，落地顺序 #3，v2 路线图外新增）：
    - **树形会话**（libs/cjcfg/src/session.cj）：`saveFork(messages, parent)`
      保存分支会话（parent=来源会话 ID，空=根）；`parentOf(id)` 读父链；
      `listTree()` 返回 (id, 摘要, parent) 三元组列表；会话 JSON 新增 `parent` 字段
    - **/tree /fork 命令**（src/main.cj handleSlashCommand）：`/tree` 调
      `store.listTree()` + `depthOf`（沿 parent 链算深度）缩进展示，`◆` 标分支点、
      `↳` 标子会话、`◀` 标当前；`/fork` 调 `store.saveFork(history, curId)` 分支新会话
    - **--mode json 无头模式**（src/main.cj `runJson`，line 580 解析入口）：
      `--mode json [--resume <id>] <prompt...>` 输出标准 JSON（reply/session_id/
      tool_calls 数组：name/args/result）；`onApproval` 默认拒绝（无头安全）；
      `CJH_MOCK=1` 走 MockProvider 验证链路
    - **验证**：PTY 实测 `/fork`→`[已分支为 s1787490534.98（parent=）]`→`/tree`
      列示正常；`CJH_MOCK=1 ./main --mode json "hello"` 输出
      `{"reply":"FINAL-DONE...","session_id":"s1787490574.994","tool_calls":[...]}`，
      exit 0
36. **V3b Provider 扩展（Ollama 本地模型）**（2026-08-23，落地顺序 #4）：
    - **OllamaProvider**（libs/cjllm/src/ollama.cj）：Ollama v0.3+ 原生支持 OpenAI 兼容
      端点 /v1/chat/completions + tools 流式，故复用 OpenAI SSE 协议
      （StreamAccumulator + SseParser），仅两点适配本地服务：
      ① endpointUrl 把 baseUrl（http://localhost:11434 或 .../v1）补全为
      /v1/chat/completions；② apiKey 为空时跳过 Authorization 头（本地默认无认证），
      apiKey 非空时仍发送（适配网关认证）。本地走 HTTP，不设置 TlsClientConfig
      （TLS 配置会导致 SNI/证书不匹配）
    - **makeProvider 接入**（src/main.cj）：`CJH_PROVIDER=ollama` 或 `base_url`
      含 localhost/127.0.0.1 → OllamaProvider；Ollama 本地服务无需认证，
      apiKey 可空（其他 provider 仍必须有 apiKey）；`CJH_PROVIDER=ollama` 但
      base_url 是云端默认时强制用 http://localhost:11434/v1
    - **/provider ollama 预设**：`/provider ollama [model] [api_key]` 写回
      base_url=http://localhost:11434/v1 + model=llama3.2（默认）+ 空 apiKey
    - **验证**：`cjpm build` 0 错误；`CJH_PROVIDER=ollama ./main --cli` 无服务时
      优雅降级为 `Connection refused`（不再误走 HTTPS/TLS）
37. **V2d 并发执行引擎**（2026-08-23，工具 DAG 并行）：
    - **Read vs. Write 分类调度**（对齐 pi/omp/anon-kode 主流方案）：
      `CjhTool.isReadOnly()` 接口方法区分纯读/有副作用工具；
      同一批 `toolCalls` 全 read-only → `spawn` 并发执行，
      `Future` 按 LLM 原始顺序 `get()` 取结果回填；
      含 state-modifying → 整批串行（避免并发写竞态）
    - **工具分类**：`read_file`/`grep`/`list_dir` = read-only（并发）；
      `bash`/`write_file`/`DeclarativeTool` = state-modifying（串行，保守默认）
    - **调度逻辑**（src/agent/loop.cj `allReadOnly` + run 循环）：
      `allReadOnly(toolCalls)` 判断批次性质；
      全 read-only 且 size>1 → ArrayList<Future<ToolResult>> 并发收集，
      按原始 toolCalls 顺序 get() 重排；否则走串行路径（保持现状）
    - **验证**：`cjpm build` 0 错误；MockProvider 改返回 2 个 read-only 工具
      （read_file + grep），`CJH_MOCK=1 --mode json "test"` 输出
      tool_calls 含 read_file/grep 两结果且顺序正确（并发执行 + 顺序重排）

38. **P0 Hashline 哈希锚点编辑**（2026-08-23，工具执行效率提升 #1）：
    - **新增 HashlineEditTool**（`src/tools/hashline.cj`）：对齐 omp Hashline 机制，
      模型用目标行内容哈希前缀标识位置（`@@{hash}`），不重打整行内容。
      文件变了哈希对不上 → patch 被拒绝（不打错地方）。
    - **patch 格式**：`@@{hash}` 锚点 + `+content` 插入 + `-content` 删除 +
      `-old +new` 替换；每行一种类型，紧跟锚点的操作作用于该锚点行。
    - **哈希算法**：FNV-1a 32-bit（无外部依赖，仓颉 crypto.digest API 不明故自实现），
      行内容 UTF-8 字节 → hash → 取低 16 位转 4 hex。
      单文件 < 1000 行碰撞概率 < 1.5%，碰撞时拒绝 patch（要求模型提供更多上下文行）。
    - **核心类**：`HashlineEditTool`（CjhTool 实现，isReadOnly=false）+
      `HashlineEditParser`（patch 文本 → PatchOp 列表）+
      `HashlineEditApplier`（定位哈希行 + 应用 Insert/Delete/Replace）+
      `HashlineHash`（FNV-1a 哈希 + locate 行匹配）+
      `ArrayListOps`（仓颉 ArrayList 无 insert/removeAt，手动构造新列表）。
    - **注册到 4 处 ToolRegistry**：verifyTests/cli/json/tui 四处都 register HashlineEditTool。
    - **效率收益**（omp 实测基准）：编辑成功率 6.7%→68.3%（10× 提升），
      输出 Token 减少 61%（重试循环消失）。
    - **构建验证**：`cjpm build` 0 错误（9 既有 warning）。
    - **状态**：✅ 编译通过 + 注册完成；待端到端测试（任务 #4）。

39. **P1 read_file 结构化摘要**（2026-08-23，工具执行效率提升 #2）：
    - **改造 ReadTool**（`src/tools/builtin.cj`）：对齐 omp read 结构化摘要，省一半 Token。
    - **默认行为**：大文件（>200 行）只返回**结构化摘要**（函数名/类名/重要注释）；
      小文件（<200 行）直接返回全文。
    - **按需展开**：`offset` + `limit` 参数指定行段（1-based），返回 `行号\t内容` 格式。
    - **符号提取**：`isSymbolDefinition()` 正则匹配仓颉/通用语言关键字
      （func/class/struct/enum/interface/prop/def/function/fn/void/int/bool/string/export）；
      `isImportantComment()` 匹配 TODO/FIXME/HACK/NOTE/WARNING/XXX。
    - **效率收益**（omp 实测）：大文件摘要 << 全文 dump，省一半上下文 Token。
    - **构建验证**：`cjpm build` 0 错误。

40. **P2 grep 目录树搜索 + gitignore 感知 + 上下文行**（2026-08-23，工具执行效率提升 #3）：
    - **改造 GrepTool**（`src/tools/builtin.cj`）：对齐 omp 进程内 ripgrep，零 fork/exec。
    - **path 支持文件/目录**：文件 → 单文件搜索；目录 → `Directory.walk` 递归搜索目录树。
    - **gitignore 感知**：`GrepWalkState.shouldIgnore()` 跳过
      .git/node_modules/target/.next/dist/build/.cache/__pycache__/.venv。
    - **上下文行**：`context` 参数控制匹配行周围 N 行（默认 0=不显示上下文）。
    - **文本文件过滤**：`isTextFile()` 按扩展名判断（.cj/.rs/.go/.py/.js/.ts/.md 等 50+ 种），
      跳过二进制文件；无扩展名的 Makefile/Dockerfile/README/LICENSE 也视为文本。
    - **GrepWalkState 类**：封装 walk 闭包的可变状态（result/totalCount/fileCount），
      解决仓颉闭包捕获可变变量报错问题。
    - **效率收益**（omp 实测）：搜索覆盖面从单文件扩展到目录树（10×），进程内零 fork。
    - **构建验证**：`cjpm build` 0 错误。

41. **输入队列方案 B + Tasks 面板 + TodoWriteTool**（2026-08-24，v1.2.0）：
    - **输入队列方案 B**（`src/tui/app.cj`）：Agent 执行期间输入框仍可编辑，
      提交时 `isAgentBusy()` 判定 busy → `enqueueInput()` 入队 + 提示
      「已入队，将在当前任务完成后发送（前面还有 N 条）」；
      Agent 执行完 `dequeueInput()` 自动处理下一条。
    - **Tasks 面板数据层**（`src/agent/loop.cj`）：`TodoItem`/`TodoStatus` +
      `Agent.addTask/updateTaskStatus/updateTaskContent/getTodos/clearTodos` +
      `onTodosChanged` 回调（TUI Tasks 面板实时刷新数据源）。
    - **Tasks 面板 UI**（`libs/cjterm/src/taskspanel.cj`）：`TasksPanel` 组件
      实时展示任务列表，渲染样式对齐 Claude Code：
      `╭─ Tasks (1 done, 1 in progress, 2 open) ───╮`
      `│  ✓ #1  输入队列：TuiApp isAgentRunning     │`
      `│  ▶ #2  main.cj onSubmit 加队列逻辑         │`
      `╰──────────────────────────────────────────────╯`
      `TaskSnapshot`/`TaskState` 与 `cjh.agent.TodoItem` 解耦（TUI 层只读渲染）。
    - **TodoWriteTool**（`src/tools/todowrite.cj`）：LLM 通过工具调用管理任务列表，
      action=add/doing/done/update/clear/list。**高内聚低耦合设计**：
      `TodoWriteTool` 留在 `cjh.tools` 包，通过函数回调操作 `Agent` 任务列表，
      避免 `cjh.tools ↔ cjh.agent` 循环依赖（符合软件设计六大原则——单向依赖+回调注入）。
    - **/task 命令**：手动操作任务列表（add/done/doing/clear/list），
      加入 SLASH_COMMANDS 候选数组 + /help 说明。
    - **UI 文案优化**：「Agent made by Cangjie」→「Harness made by Cangjie」（7 处）。
    - **真实 LLM 端到端验证**（DeepSeek via taotoken `deepseek-v4-flash`）：
      LLM 成功调用 `todo_write` 工具——`add` × 3（读取文件/分析内容/生成报告）
      + `doing` × 1（标记 #1 为进行中）+ `list` × 1（列出所有任务）。
    - **构建验证**：`cjpm build` 0 错误。

## 二、关键文件
- `src/main.cj` — 入口（--mock/--cli/--resume/--list/TUI）+ makeProvider + TuiBridge
- `src/tui/app.cj` — TUI 应用（全量重绘+锁）；`src/tui/markdown.cj` — markdown 渲染
- `src/{agent,tools}/` — 核心包
- `libs/cjterm/` — ansi/screen/term(纯FFI)/logo/picker(ListPicker)/component(Component接口+TuiCanvas)/input/outputview/statusbar/**spinner(流式指示器)/confirm(审批弹窗)/editor(多行编辑器)/tabbar(标签栏)/box(边框容器)**
- `libs/cjllm/` — provider/openai/anthropic/sse/mock/mojibake/message/llm_test
- `libs/cjcfg/` — config/session/env_ffi/**capability（三域安全模型+glob+审批判定）/capability_test（6 用例）**
- `docs/` — 方案与架构设计-v2 / 模块规划 / 开发文档与踩坑记录 / 实现方案与交接 / 进度记录 / **对标pi差距分析（2026-08-22：与 pi.dev 能力差距 + 差异化优势 + 落地顺序 V2c→V2b→树形会话/--mode json）**

## 三、当前 todo（按 v2 路线图）

**已确认落地顺序**（2026-08-22 对标 pi 差距分析，见 docs/对标pi差距分析.md）：
```
1. V2c 记忆分层（Compaction/AGENTS.md）   ← 上下文工程，pi 最核心
2. V2b 插件系统（Skills/扩展点）          ← 生态入口
3. 树形会话 + --mode json                ← 低成本高感知
4. V3b Provider 扩展（Ollama 本地模型）   ← 解锁离线场景（后续）
```

- [x] V1：P0+P1 全部（agent/工具/双协议/TUI/会话/配置）
- [x] UI 多轮打磨（logo/配色/markdown/输入/焦点/乱码）
- [x] V2a 三域安全（命令/工具/资源白名单 + 审批链）—— 立身之本，优先 ✅（capability.cj + TUI/CLI 审批链 + 6 单测 + mock 端到端）
- [x] V2c 记忆分层（Compaction 摘要压缩 + AGENTS.md 项目指令）—— 落地顺序 #1 ✅
  （Agent.maybeCompact 阈值触发 LLM 摘要 + compact(force) 手动压缩；
   loadProjectInstructions 逐级向上 8 层查找 AGENTS.md → buildSystemPrompt 注入；
   /compact 命令接线；cjpm build 0 错误；PTY 实测 /help 含 /compact、/compact 触发压缩）
- [x] V2b 插件系统（plugin.json + 声明式工具 + Skills + 扩展点）—— 落地顺序 #2 ✅
  （Skills 即文件：~/.cjh/skills/<name>.md frontmatter + 正文指令；
   parseFrontmatter 多行 JSON 解析；enabledSkills 白名单；DeclarativeTool
   shell 模板工具；/skills 命令；system prompt 注入；端到端验证通过）
- [x] 树形会话 + --mode json —— 落地顺序 #3（v2 路线图外新增）✅
  （session.cj saveFork/parentOf/listTree 树形分支；main.cj /tree 列示、
   /fork 分支、depthOf 深度计算、--mode json 无头模式 runJson；
   PTY 实测 /fork→[已分支为...]→/tree 列示正常；
   CJH_MOCK=1 --mode json "hello" 输出标准 JSON：reply/session_id/tool_calls）
- [x] V2d 并发执行引擎（工具 DAG 并行）✅
  （CjhTool.isReadOnly() 分类 read-only/state-modifying；
   全 read-only 批次 spawn 并发 + Future 按原始顺序 get() 重排；
   含 state-modifying 整批串行；对齐 pi/omp/anon-kode Read vs. Write 方案）
- [ ] V2e IM 网关（Channel 抽象 + Web 渠道 + 审批远程化）
- [x] V3b Provider 扩展（Ollama 本地模型）—— 落地顺序 #4 ✅
  （OllamaProvider libs/cjllm/src/ollama.cj：复用 OpenAI SSE 协议，
   endpointUrl 补全 /v1/chat/completions，apiKey 空时跳过 Authorization，
   本地走 HTTP 不设 TLS；makeProvider 接入 CJH_PROVIDER=ollama 或
   base_url 含 localhost/127.0.0.1；/provider ollama [model] 预设；
   无服务时优雅降级 Connection refused）
- [ ] V3 信任链/协议深化；V4 多 agent + 鸿蒙

## 四、运行
```bash
cd /root/test/cj/cjh
source /root/test/cj/tauri_cj/cj-env.sh
cjpm build        # 纯仓颉，无需 C 桥
cd target/release/bin && ./main    # TUI（RPATH 已内嵌，无需 LD_LIBRARY_PATH）
```
- 配置：~/.cjh/（首次自动生成模板）；DeepSeek key 已配
- 测试：--mock（工具链）/ --cli / --list / --resume

## 五、已知限制/待改进
- 流式中间帧渲染有历史残留（真实终端最终画面正确，PTY 抓取可见）
- 代码块（```）高亮有边界情况（单 ` 与 ``` 状态机）
- 无多行输入/方向键历史；单会话无并发
- 中文宽度（CJK=2 列）未精确处理（光标微偏）
- capability：`net` 网络白名单已解析未接入（当前无网络工具）；CLI 审批在
  管道/脚本输入下 `stdin.read` 可能返回 0 即默认拒绝（真实 TTY 逐行交互正常）；
  TUI 审批是主要交互路径（requestApproval 阻塞等 y/n）
- V2a 后续：命令白名单 token 通配目前只支持 `*` 后缀（`system*`），
  `bash:write` 写命令判定为关键词匹配（`>`/`rm`/`tee` 等），可再细化
