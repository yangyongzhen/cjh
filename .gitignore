# 构建产物
target/
*.o
*.a
*.so
libs/*/target/

# 本地环境
cjpm.lock
*.stdx.bak

# 本地 agent 配置
.atomcode/

# 本地运行产物（日志/临时文件）
.cjh/
cjh_tui.log

# 系统文件
.DS_Store
build-script-cache/
cangjie-stdx-windows-*.zip

# Python
__pycache__/

# dist 交付产物：逐版本归档包与打包暂存目录只做本地留存，不进版本库。
# 不写死版本号；已纳管的文件不受忽略规则影响，需要纳管某个包时用 git add -f。
dist/windows-*/
dist/linux/
dist/cjh-v*-x64*.zip
