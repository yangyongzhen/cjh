# cjh：仓颉语言 Coding Agent Harness 方案

> 日期：2026-08-21
> 状态：已决策启动，P0 实施中
> 定位：用仓颉语言从零实现的交互式编码代理（coding agent）

---

## 一、项目定位

**cjh = Cangjie Harness**：一个用仓颉语言从零实现的终端编码代理。

核心能力：用户在终端里用自然语言描述任务（如"修改某文件的某函数"），agent 理解意图、自主规划、调用工具（执行命令、读写文件）、观察结果、迭代直到完成——全程在终端 TUI 中呈现，支持会话持久化与后续扩展。

## 二、为什么用仓颉

- **单二进制交付**：仓颉 cjnative 静态编译，无运行时依赖，对标"一个可执行文件走天下"的体验；
- **性能与体积**：静态编译 + 低内存占用，终端交互零卡顿；
- **强类型**：工具协议、消息状态、LLM 响应解析全程类型安全；
- **生态卡位**：agent 工具链在仓颉生态是空白，先做者定义范式；
- **鸿蒙延展**：未来可平滑接入鸿蒙场景。

## 三、总体架构

```
┌─────────────────────────────────────────────┐
│              终端 TUI（tui/）                │
│   差分渲染 + 按键捕获 + 编辑器 + 补全         │
└───────────────┬─────────────────────────────┘
┌───────────────▼─────────────────────────────┐
│           Agent 运行时（agent/）             │
│   主循环：消息状态机 + 工具调用协议 + 上下文压缩│
└───────────────┬─────────────────────────────┘
┌───────────────▼─────────────────────────────┐
│          工具集（tools/）                    │
│   bash 执行 / 文件读 / 文件编辑 / 搜索        │
└───────────────┬─────────────────────────────┘
┌───────────────▼─────────────────────────────┐
│        LLM 提供商层（llm/）                  │
│   OpenAI 兼容协议 + SSE 流式 + 多提供商抽象    │
└─────────────────────────────────────────────┘
```

### 核心机制设计

1. **Agent Loop**：系统提示 + 消息历史 → LLM → 模型输出（文本或工具调用）→ 执行工具 → 结果回填 → 下一轮，直到模型给出最终答复；
2. **工具调用协议**：LLM 输出结构化工具请求（JSON），harness 校验参数、分发执行、返回结果；
3. **差分渲染 TUI**：维护期望屏幕状态，与真实终端对比只输出差异行，实现流畅的流式渲染；
4. **会话持久化**：对话历史存 SQLite，重启可续；
5. **Skills 自扩展**（P2）：agent 可创建/修改技能文件，实现自我扩展。

## 四、仓颉能力对照（已验证）

| 需求 | 仓颉方案 | 状态 |
|---|---|---|
| HTTP/HTTPS（LLM API） | `stdx.net.http.Client` | ✅ 已装配 |
| JSON 解析/序列化 | `stdx.encoding.json` | ✅ 已装配 |
| SSE 流式解析 | 手写流解析（按行读 `data:` 帧） | 🔨 P0 |
| 正则（搜索/工具） | `std.regex` | ✅ |
| 文件系统 | `std.fs` | ✅ |
| 进程执行（bash） | `std.process` | ✅ |
| SQLite 会话 | `std.database.sql` | ✅ 库存在 |
| 并发 | 仓颉 M:N 线程 + `spawn` | ✅ |
| 终端 ANSI 渲染 | 自研（P0） | 🔨 |
| termios 原始模式 | C FFI（已有 C 桥范式） | 🔨 P0 |

## 五、目录结构

```
cjh/
├── cjpm.toml            # 主包（executable）
├── src/
│   ├── main.cj          # CLI 入口
│   ├── config.cj        # 配置（模型/提供商/密钥来源）
│   ├── llm/
│   │   ├── provider.cj  # LLM 抽象（chat 请求/流式响应）
│   │   ├── openai.cj    # OpenAI 兼容协议
│   │   └── sse.cj       # SSE 流解析
│   ├── agent/
│   │   ├── loop.cj      # agent 主循环
│   │   ├── message.cj   # 消息模型（system/user/assistant/tool）
│   │   └── tools.cj     # 工具注册与分发
│   ├── tools/
│   │   ├── bash.cj      # bash 执行
│   │   ├── read.cj      # 文件读取
│   │   └── edit.cj      # 文件编辑
│   └── tui/
│       ├── screen.cj    # 屏幕状态树
│       ├── render.cj    # 差分渲染
│       └── term.cj      # termios FFI（原始模式/按键）
└── docs/                # 设计文档 + 踩坑记录
```

## 六、实施计划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **P0 MVP** | LLM 提供商层（OpenAI 协议 + SSE）+ agent loop + 3 工具（bash/read/edit）+ 基础 TUI | 终端输入自然语言任务 → agent 自主调用工具完成并展示过程 |
| **P1** | 完整 TUI（差分渲染/编辑器/补全）+ 工具集扩展 + SQLite 会话 | 交互体验接近主流 agent；重启可续会话 |
| **P2** | 多提供商（Anthropic/Google）+ skills 自扩展 + 配置系统 | 可配置任意提供商；agent 可自我扩展技能 |
| **P3** | 远端协议（client/server）、插件体系 | 支持远程 agent 会话 |

**P0 里程碑**（当前）：2 周内交付可用 MVP。

## 七、风险与对策

| 风险 | 对策 |
|---|---|
| TUI 差分渲染精细、调试耗时 | 先做最小可用（滚动 + 彩色 + 流式输出），迭代优化 |
| LLM 提供商 API 演进 | 协议层抽象隔离，单文件适配 |
| 仓颉无现成终端库 | 自研 ANSI + termios FFI（C 桥范式已验证） |
| 协程栈对阻塞式 C 调用 | 阻塞操作放原生线程（tauri_cj 已验证的 C 桥方案） |

## 八、验证方式

- P0 验收：真实终端中运行 `cjh`，下达文件修改任务，观察 agent 工具调用链与最终结果；
- 单测：SSE 解析、JSON 协议、工具参数校验（仓颉 `@Test`）；
- 无密钥环境：提供 `--mock` 模式用脚本模拟 LLM 响应，便于 CI/离线验证。
